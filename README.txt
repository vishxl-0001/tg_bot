Telegram Image Bot

1. Put up to 10 images inside the images folder.
2. Create a bot using BotFather and get the token.
3. On Render, set environment variable:
   BOT_TOKEN=your_token_here
4. Build command:
   pip install -r requirements.txt
5. Start command:
   python bot.py
